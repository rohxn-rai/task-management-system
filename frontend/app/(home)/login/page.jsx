import LoginPage from "./(login)";

export const metadata = {
  title: "Task Management System | Login",
  description: "HomePage",
};

const LoginPageLayout = () => {
  return (
    <>
      <LoginPage />
    </>
  );
};

export default LoginPageLayout;
